import React from "react";
import styled from "styled-components";

export default () => {
    return <ProfileSettings>Profile Settings</ProfileSettings>;
};

const ProfileSettings = styled.div`
    padding: 2% 5%;
`;
